---
type: concept
topics: [reinforcement-learning]
status: evergreen
created: 2026-08-29
aliases: [RL, the RL loop, generalized policy iteration, GPI]
---

# Reinforcement Learning

Learning to act from evaluative feedback: no correct answers, only rewards; consequences delayed; and — the defining twist — **the data is generated by your own behavior**. ([[Markov Decision Process|Why that's harder than supervised learning]].) This is the hub of the RL cluster: it shows how the foundation notes fit into one machine. Read this first; each cell links to the note that owns it.

## The master loop

All of RL is one loop, run at some granularity — the field's name for it is **generalized policy iteration** ([[Reinforcement Learning - An Introduction (1998)|Sutton & Barto]]):

$$\Large \text{ACT} \;\longrightarrow\; \text{EVALUATE} \;\longrightarrow\; \text{IMPROVE} \;\longrightarrow\; \circlearrowleft$$

| stage | the question it answers | owned by | key objects |
|---|---|---|---|
| **the arena** | what problem are we solving at all? | [[Markov Decision Process]] | $(\mathcal{S}, \mathcal{A}, P, R, \gamma)$, return, policy |
| **ACT** | which actions generate the data — and how much do we deviate *on purpose*? | [[Exploration vs Exploitation]] | ε-greedy, softmax/temperature, UCB, novelty bonuses |
| **EVALUATE** | how good is current behavior? | [[Temporal Difference Learning]] — from experience; [[Bellman Equation]] — on paper, when the model is known | $V^\pi$, $Q^\pi$, TD error $\delta$ |
| **IMPROVE** | how do value estimates become a better policy? | [[Bellman Equation]]'s greedy map (value-based) · [[Policy Gradient]] (policy-based) | $\arg\max_a Q$, $\nabla_\theta J$ |

Three placements that are easy to get wrong (and that the notes argue in full):

- **Exploration belongs to ACT, not IMPROVE.** Improvement is *pure exploitation* of the current estimates (act greedier / follow the gradient). Exploration is the deliberate corruption applied when *acting*, so that the next round of evaluation has data covering enough of the world — it's the tax that keeps improvement possible, not the improvement itself
- **TD computes *any* policy's values, not just optimal ones.** The target decides what you get: $r + \gamma V(s')$ under π → $V^\pi$ (pure evaluation); $r + \gamma\max_{a'}Q(s',a')$ → $Q^*$ directly (Q-learning collapses IMPROVE into the target). Optimality is a property of the target, not of the TD idea
- **The loop needn't run in phases.** Tabular [[Q-Learning]] does all three stages *every timestep*: act ε-greedily, TD-update one entry, and — since the policy is *defined* as greedy(Q) — improvement happens the instant the entry changes

## The fork before the loop: planning vs learning

| you have | you do | note |
|---|---|---|
| the full model $(P, R)$ as tables | **planning** — pure computation, no acting, no exploration concept; value/policy iteration | [[Bellman Equation]] |
| only the ability to act and observe | **learning** — the same Bellman backups rebuilt from one sampled transition at a time | [[Temporal Difference Learning]] |

Learning inherits everything from planning *plus* two new problems: sampling noise (→ step sizes, variance) and **coverage** — you only learn about what you visit, so exploration stops being optional and becomes a convergence precondition ([[Exploration vs Exploitation]]).

## Two bodies, one skeleton

The loop has two main implementations, split by *where the policy lives*:

| | policy is | IMPROVE step | explores via | notes |
|---|---|---|---|---|
| **value-based** | implicit: greedy($Q$) | free — greedy($Q$) moves when $Q$ moves | ε-greedy / bonuses bolted on | [[Q-Learning]] → [[Deep Q-Network]] |
| **policy-based** | an explicit network $\pi_\theta$ | gradient ascent on performance | the policy's *own* randomness + entropy bonus | [[Policy Gradient]] → [[Actor-Critic]] → [[PPO]] |

**LLM post-training runs on the second row**: the critic TD-evaluates the current policy (EVALUATE), the policy gradient updates it (IMPROVE), and the sampling temperature + entropy term keep exploration alive (ACT) — [[RLHF]], [[GRPO]], [[RLVR]].

## Reading order

1. [[Markov Decision Process]] — the problem
2. [[Bellman Equation]] — solving it with the model (and the value↔policy duality everything reuses)
3. [[Temporal Difference Learning]] — solving it from experience
4. [[Exploration vs Exploitation]] — supplying the experience

Then branch by interest: value-based ([[Q-Learning]], [[Deep Q-Network]]) · policy-based ([[Policy Gradient]], [[Actor-Critic]], [[PPO]]) · search ([[Monte Carlo Tree Search]]) · LLMs ([[RLHF]], [[DPO]], [[GRPO]], [[RLVR]]).

## Sources

- [[Reinforcement Learning - An Introduction (1998)]] — the GPI framing; the loop *is* this book's thesis

---
Hub of the RL cluster — map: [[RL MOC]].
